Skip to content
Navigation Menu
mujtabaa11
cv-html

Type / to search
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
Deploy to S3
Update deploy.yml #3
Jobs
Run details
Workflow file for this run
.github/workflows/deploy.yml at 6ee4c3e
name: Deploy to S3

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: arn:aws:iam::307946668157:role/cv-html-repo-accessRole
          aws-region: us-east-1

      - name: Debug AWS Identity
        run: aws sts get-caller-identity
      
      - name: Deploy to S3
        run: |
          aws s3 sync . s3://mohamedmujtaba --delete
Update deploy.yml · mujtabaa11/cv-html@6ee4c3e 
